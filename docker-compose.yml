version: "3.9"

services:
  pipeline:
    build: .
    volumes:
      - ./data:/app/data
      - ./configs:/app/configs
    env_file:
      - .env
    command: >
      python run_pipeline.py
      --pdf data/raw/sample.pdf
      --run-name paper_run_001
